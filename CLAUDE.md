# CLAUDE.md

Agent guidance for this repo lives in @AGENTS.md (single source of truth, shared across
tools). It covers the two-tier rule model, where things live, how to add a rule, and —
most importantly — the golden-file regeneration + `EXPECTED`-table workflow to run after
any rule change.
